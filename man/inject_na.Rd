% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_imp.R
\name{inject_na}
\alias{inject_na}
\title{Inject NA Values into a Matrix}
\usage{
inject_na(
  obj,
  num_na,
  rowmax = 0.9,
  colmax = 0.9,
  check_sd = FALSE,
  max_iter = 1000
)
}
\arguments{
\item{obj}{A numeric matrix with \strong{samples in rows} and \strong{features in columns}.}

\item{num_na}{The number of missing values to inject.}

\item{rowmax}{Number between 0 and 1. NA injection cannot create rows with a higher proportion of
missing values than this threshold.}

\item{colmax}{Number between 0 and 1. NA injection cannot create columns with a higher proportion of
missing values than this threshold.}

\item{check_sd}{Logical. If \code{TRUE}, also ensure that no column becomes zero-variance after injection.}

\item{max_iter}{Maximum number of iterations to attempt finding valid NA positions (default \code{1000}).}
}
\value{
A vector of integer indices indicating the positions in the matrix
where NAs should be injected.
}
\description{
This helper function randomly selects positions in a matrix to inject a specified number of NA values,
ensuring that the injection does not exceed specified missingness thresholds for rows and columns.
It attempts to find a valid set of positions within a maximum number of iterations.
}
\details{
The function first checks that the existing missingness in \code{obj} already respects the \code{rowmax}
and \code{colmax} thresholds. It then repeatedly samples random positions from the currently non-NA
elements and verifies that adding the new NAs would not violate the row/column missingness limits
(or create zero-variance columns when \code{check_sd = TRUE}). Sampling continues until a valid set of
positions is found or \code{max_iter} is reached (in which case an error is thrown).
}
\examples{
#' set.seed(123)
#'
#' # Create a sample matrix (no existing NAs)
#' mat <- matrix(rnorm(100), nrow = 20, ncol = 5)
#'
#' # Basic usage - inject 10 NAs (defaults: rowmax/colmax = 0.9, check_sd = FALSE)
#' na_loc <- inject_na(mat, num_na = 10)

}
