% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_imp.R
\name{sample_na_loc_stratified}
\alias{sample_na_loc_stratified}
\title{Stratified Sampling of NA Locations}
\usage{
sample_na_loc_stratified(obj, n_cols, n_rows, n_reps, subset = NULL)
}
\arguments{
\item{obj}{A numeric matrix with \strong{samples in rows} and \strong{features in columns}.}

\item{n_cols}{Integer. Number of columns to sample per replication.}

\item{n_rows}{Integer. Number of rows to sample \strong{per selected column}.}

\item{n_reps}{Integer. Number of independent replications to generate.}

\item{subset}{Integer or character vector (optional). Column indices (or
column names) to sample from. If supplied, \code{n_cols} columns are randomly
sampled from this pool for \strong{each} replication (instead of from all
columns). If \code{NULL} (default), samples from all columns.}
}
\value{
A list of length \code{n_reps}. Each element is an integer matrix with
two columns named \code{"row"} and \code{"col"} containing the 1-based row and
column indices of the positions where NAs should be injected. Each matrix
has exactly \code{n_rows * n_cols} rows.
}
\description{
Randomly samples matrix positions for NA injection in a \strong{stratified} manner
(balanced by column). For each replication, it randomly selects \code{n_cols}
columns (from all columns or the restricted \code{subset} if supplied) and then
samples exactly \code{n_rows} row positions \strong{within each} of those columns.
}
\examples{
set.seed(123)

# Create a sample matrix (complete data)
mat <- matrix(rnorm(1000), nrow = 100, ncol = 10)

# Sample 2 columns with 15 NAs each, repeated 5 times
na_locs <- sample_na_loc_stratified(mat, n_cols = 2, n_rows = 15, n_reps = 5)

# Check structure
length(na_locs) # 5 replications
nrow(na_locs[[1]]) # 30 positions per replication
table(na_locs[[1]][, "col"]) # exactly 15 per column

# Example usage: inject NAs into a test matrix
mat_test <- mat
loc <- na_locs[[1]]
mat_test[cbind(loc[, "row"], loc[, "col"])] <- NA

# Using a restricted pool of columns (still randomly samples n_cols from
# the pool for each replication)
na_locs_fixed <- sample_na_loc_stratified(
  mat,
  n_cols = 2,
  n_rows = 10,
  n_reps = 3,
  subset = c(1, 5, 8)
)
}
\seealso{
\code{\link[=sample_na_loc]{sample_na_loc()}}
}
