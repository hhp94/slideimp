% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slideimp_registry.R
\name{register_group_resolver}
\alias{register_group_resolver}
\title{Register a Group Resolver}
\usage{
register_group_resolver(resolver)
}
\arguments{
\item{resolver}{A function that takes the manifest character and returns
a \code{data.frame}.}
}
\value{
Invisibly returns \code{NULL}. The function is called only for its
side effect of registering the resolver.
}
\description{
Called by the companion package \code{slideimp.extra} from their \code{.onLoad()}
to register a callback that turns character manifest into a \code{data.frame} for
\code{group_imp()}/\code{prep_groups()}.
}
\examples{
# Typically called from `.onLoad` by `slideimp.extra` package, not by users directly.
dummy_resolver <- function(x) data.frame(feature = character(), group = character())
register_group_resolver(dummy_resolver)
}
