% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mat_miss.R
\name{mat_miss}
\alias{mat_miss}
\title{Column or Row Missing Count (or Proportion)}
\usage{
mat_miss(obj, col = TRUE, prop = FALSE)
}
\arguments{
\item{obj}{A numeric matrix.}

\item{col}{Logical. If \code{TRUE} (default), compute per-column statistics;
if \code{FALSE}, compute per-row statistics.}

\item{prop}{Logical. If \code{FALSE} (default), return raw missing counts;
if \code{TRUE}, return missing proportion.}
}
\value{
A named numeric vector of either the column or row missing count or
proportion.
}
\description{
Calculate the number (or proportion) of missing values per column or per row
of a numeric matrix without realizing the full mask matrix.
}
\examples{
mat <- matrix(c(1, NA, 3, 4, NA, 6, NA, 8, 9), nrow = 3)
mat

# column missing counts (default)
mat_miss(mat)

# row missing counts
mat_miss(mat, col = FALSE)

# column missing proportions
mat_miss(mat, prop = TRUE)

}
